# =============================================================
# VIGÍLIA — Dependências da interface Streamlit (execução local)
# A versão de produção roda no Firebase (ver functions/requirements.txt)
# =============================================================

streamlit==1.57.0
requests==2.34.2
beautifulsoup4==4.14.3
