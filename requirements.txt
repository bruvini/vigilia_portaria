# =============================================================
# VIGÍLIA — Dependências do Projeto
# Plataforma Institucional de Monitoramento de Diários Oficiais
# Secretaria Municipal da Saúde de Joinville
# Atualizado em: 2026-05-24
# =============================================================

# --- CORE WEB / UI ---
streamlit==1.57.0
watchdog==6.0.0

# --- DADOS E ANÁLISE ---
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
narwhals==2.21.2

# --- HTTP / REQUISIÇÕES ---
requests==2.34.2
urllib3==2.7.0
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.15

# --- PARSING HTML / PDF ---
beautifulsoup4==4.14.3
soupsieve==2.8.3
lxml
pdfminer.six==20251230
pdfplumber
pypdfium2==5.8.0
pillow==12.2.0

# --- INTEROPERABILIDADE FHIR / HL7 ---
# uuid, base64 e json são módulos da stdlib Python — sem dependência extra.
# Para futura integração com servidor de terminologia SNOMED CT (Ontoserver/Snowstorm),
# adicionar: fhir.resources>=7.1.0

# --- SERIALIZAÇÃO / VALIDAÇÃO ---
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==0.30.0
attrs==26.1.0

# --- STREAMLIT INTERNALS ---
altair==6.1.0
pydeck==0.9.2
blinker==1.9.0
cachetools==7.1.3
packaging==26.2
click==8.4.0
toml==0.10.2
MarkupSafe==3.0.3
Jinja2==3.1.6
itsdangerous==2.2.0
protobuf==7.35.0
typing_extensions==4.15.0

# --- ASSINATURAS E CRIPTOGRAFIA ---
cryptography==48.0.0
cffi==2.0.0
pycparser==3.0

# --- GIT INTEGRATION (verify.py e CI) ---
GitPython==3.1.50
gitdb==4.0.12
smmap==5.0.3

# --- SERVIDOR ASGI / WEBSOCKETS ---
uvicorn==0.47.0
starlette==1.0.0
websockets==16.0
httptools==0.7.1
h11==0.16.0

# --- UTILITÁRIOS ASYNC ---
anyio==4.13.0
greenlet==3.5.0
pyee==13.0.1

# --- DATAS ---
python-dateutil==2.9.0.post0
six==1.17.0

# --- FORM / MULTIPART ---
python-multipart==0.0.29

# --- RESILIÊNCIA HTTP ---
tenacity==9.1.4
